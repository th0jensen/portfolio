{
  "name": "cli",
  "sourceRoot": "cli/src",
  "targets": {
    "build": {
      "command": "cargo build --release",
      "options": { "cwd": "backend" }
    },
    "lint": {
      "command": "cargo clippy -- -D warnings",
      "options": { "cwd": "cli" }
    }
  }
}
